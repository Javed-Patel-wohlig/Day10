
    async function RecursiveInsert(url) {
          const bulk = result.map((item) => {
            return {
              insertOne: {
                document: {
                  name: item.name,
                  url: item.url,
                },
              },
            };


          await myModel3.bulkWrite(bulk);


      totoalPages--;
      if (totoalPages === 0) {
        res.status(200).send("Done");
        return;
      }
      RecursiveInsert(next)
    }
    RecursiveInsert(url);